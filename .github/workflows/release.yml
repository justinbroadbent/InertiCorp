name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test
        run: dotnet test --configuration Release --no-build --verbosity normal

  build-windows:
    name: Build Windows Release
    needs: test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.5.1
          use-dotnet: true
          include-templates: true

      - name: Restore dependencies
        run: dotnet restore

      - name: Verify Godot setup
        run: |
          echo "GODOT: $GODOT"
          echo "Godot version:"
          "$GODOT" --version
        shell: bash

      - name: Download CUDA Runtime and cuBLAS DLLs
        run: |
          # Download CUDA runtime DLLs from llama.cpp release
          $cudaUrl = "https://github.com/ggml-org/llama.cpp/releases/download/b7616/cudart-llama-bin-win-cuda-12.4-x64.zip"
          $cudaZip = "cuda-runtime.zip"

          Write-Host "Downloading CUDA runtime DLLs from: $cudaUrl"
          Invoke-WebRequest -Uri $cudaUrl -OutFile $cudaZip

          # Extract to a temp directory
          $cudaDir = "cuda-runtime"
          Expand-Archive -Path $cudaZip -DestinationPath $cudaDir -Force

          # Create native cuda12 directory
          $targetDir = "src/InertiCorp.Core/native/cuda12"
          New-Item -ItemType Directory -Force -Path $targetDir

          # Copy the runtime DLLs
          Get-ChildItem $cudaDir -Filter "*.dll" -Recurse | Copy-Item -Destination $targetDir -Force

          # Download cuBLAS from NVIDIA redistributables (required for GPU matrix operations)
          # These are large (~500MB) but essential for GPU inference
          $cublasUrl = "https://developer.download.nvidia.com/compute/cudnn/redist/cudnn/windows-x86_64/cudnn-windows-x86_64-9.0.0.312_cuda12-archive.zip"

          # Alternative: Use pre-extracted cuBLAS from a llama.cpp CUDA build
          # The llama.cpp releases include cuBLAS in their full CUDA builds
          $llamaCudaUrl = "https://github.com/ggml-org/llama.cpp/releases/download/b7616/llama-b7616-bin-win-cuda-12.4-x64.zip"
          $llamaCudaZip = "llama-cuda.zip"

          Write-Host "Downloading llama.cpp CUDA build (includes cuBLAS)..."
          Invoke-WebRequest -Uri $llamaCudaUrl -OutFile $llamaCudaZip -TimeoutSec 600

          $llamaCudaDir = "llama-cuda"
          Expand-Archive -Path $llamaCudaZip -DestinationPath $llamaCudaDir -Force

          Write-Host "Extracted llama.cpp CUDA contents:"
          Get-ChildItem $llamaCudaDir -Recurse -Filter "*.dll" | ForEach-Object { Write-Host "  $($_.Name) ($([math]::Round($_.Length / 1MB, 1)) MB)" }

          # Copy cuBLAS DLLs (cublas64_12.dll, cublasLt64_12.dll)
          Get-ChildItem $llamaCudaDir -Filter "cublas*.dll" -Recurse | Copy-Item -Destination $targetDir -Force

          Write-Host "CUDA DLLs installed:"
          Get-ChildItem $targetDir | ForEach-Object { Write-Host "  $($_.Name) ($([math]::Round($_.Length / 1MB, 1)) MB)" }
        shell: pwsh

      - name: Generate .NET bindings
        run: |
          "$GODOT" --headless --path src/InertiCorp.Game --build-solutions --quit || true
        shell: bash

      - name: Build .NET project
        run: dotnet build --configuration Release

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Create build directory
        run: New-Item -ItemType Directory -Force -Path build
        shell: pwsh

      - name: Export Game
        run: |
          echo "Exporting to: ${{ github.workspace }}/build/InertiCorp.exe"
          "$GODOT" --headless --verbose --path src/InertiCorp.Game --export-release "Windows Desktop" "${{ github.workspace }}/build/InertiCorp.exe"
        shell: bash

      - name: Copy .NET assemblies to build
        run: |
          $dataDir = "build\data_InertiCorp.Game_windows_x86_64"
          New-Item -ItemType Directory -Force -Path $dataDir

          # The ExportRelease directory contains win-x64 subdirectory with all assemblies
          # Copy the contents of win-x64 directly to the data directory (not the win-x64 folder itself)
          $exportDir = "src\InertiCorp.Game\.godot\mono\temp\bin\ExportRelease\win-x64"
          if (Test-Path $exportDir) {
            Copy-Item "$exportDir\*" $dataDir -Recurse -Force
            Write-Host "Copied assemblies from: $exportDir"
          } else {
            Write-Host "ExportRelease\win-x64 not found, checking other locations..."

            # Fallback to Release directory
            $monoDir = "src\InertiCorp.Game\.godot\mono\temp\bin\Release"
            if (Test-Path $monoDir) {
              Copy-Item "$monoDir\*" $dataDir -Recurse -Force
              Write-Host "Copied assemblies from: $monoDir"
            }
          }

          Write-Host "Data directory file count:"
          (Get-ChildItem $dataDir -File).Count
          Write-Host "Sample files:"
          Get-ChildItem $dataDir -File | Select-Object -First 10 Name
        shell: pwsh

      - name: Copy native DLLs to cuda12 directory
        run: |
          $dataDir = "build\data_InertiCorp.Game_windows_x86_64"
          $cuda12Dir = "$dataDir\runtimes\win-x64\native\cuda12"

          # Ensure cuda12 directory exists
          New-Item -ItemType Directory -Force -Path $cuda12Dir

          # IMPORTANT: Use CUDA-enabled DLLs from llama.cpp download, NOT from NuGet!
          # The NuGet package includes CPU-only versions that don't actually use GPU.
          $llamaCudaDir = "llama-cuda"

          # Copy CUDA-enabled native DLLs from the downloaded llama.cpp CUDA build
          $nativeDlls = @("llama.dll", "ggml.dll", "ggml-base.dll", "ggml-cpu.dll", "ggml-cuda.dll")
          foreach ($dll in $nativeDlls) {
            # Find the DLL in the llama-cuda directory (may be in subdirectory)
            $srcFile = Get-ChildItem $llamaCudaDir -Filter $dll -Recurse | Select-Object -First 1
            if ($srcFile) {
              Copy-Item $srcFile.FullName $cuda12Dir -Force
              # Also remove any CPU-only version from data root to prevent confusion
              $dataRootDll = "$dataDir\$dll"
              if (Test-Path $dataRootDll) {
                Remove-Item $dataRootDll -Force
                Write-Host "Removed NuGet CPU version: $dll from data root"
              }
              Write-Host "Copied CUDA-enabled $dll ($([math]::Round($srcFile.Length / 1MB, 1)) MB) to cuda12"
            } else {
              Write-Host "Warning: $dll not found in llama-cuda download"
            }
          }

          # Copy cuBLAS DLLs from the downloaded CUDA package
          $sourceDir = "src/InertiCorp.Core/native/cuda12"
          $cublasDlls = @("cublas64_12.dll", "cublasLt64_12.dll", "cudart64_12.dll")
          foreach ($dll in $cublasDlls) {
            $srcPath = "$sourceDir\$dll"
            if (Test-Path $srcPath) {
              Copy-Item $srcPath $cuda12Dir -Force
              Write-Host "Copied $dll to cuda12"
            } else {
              Write-Host "Warning: $dll not found in source directory"
            }
          }

          Write-Host "cuda12 directory contents:"
          Get-ChildItem $cuda12Dir | ForEach-Object { Write-Host "  $($_.Name) ($([math]::Round($_.Length / 1MB, 1)) MB)" }
        shell: pwsh

      - name: List build directory
        run: ls -laR build/
        shell: bash

      - name: Verify export succeeded
        run: |
          if [ ! -f "build/InertiCorp.exe" ]; then
            echo "Export failed - InertiCorp.exe not found"
            exit 1
          fi
          echo "Export succeeded! File count:"
          find build -type f | wc -l
        shell: bash

      - name: Create ZIP archive
        run: |
          Compress-Archive -Path build\* -DestinationPath "InertiCorp-${{ steps.version.outputs.VERSION }}-Windows.zip"
        shell: pwsh

      - name: Install Inno Setup
        run: choco install innosetup -y

      - name: Update version in installer script
        run: |
          $content = Get-Content installer/InertiCorp.iss -Raw
          $content = $content -replace '#define MyAppVersion ".*"', '#define MyAppVersion "${{ steps.version.outputs.VERSION }}"'
          Set-Content installer/InertiCorp.iss $content
        shell: pwsh

      - name: Build Installer
        run: |
          # Update source path to use absolute path
          $issContent = Get-Content installer/InertiCorp.iss -Raw
          $issContent = $issContent -replace 'Source: "\.\.\\build\\\*"', "Source: `"${{ github.workspace }}\build\*`""
          Set-Content installer/InertiCorp.iss $issContent
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /O"." /F"InertiCorp-${{ steps.version.outputs.VERSION }}-Setup" installer/InertiCorp.iss
        shell: pwsh

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            InertiCorp-${{ steps.version.outputs.VERSION }}-Windows.zip
            InertiCorp-${{ steps.version.outputs.VERSION }}-Setup.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    name: Build Linux Release
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.5.1
          use-dotnet: true
          include-templates: true

      - name: Restore dependencies
        run: dotnet restore

      - name: Generate .NET bindings
        run: godot --headless --path src/InertiCorp.Game --build-solutions --quit || true

      - name: Build .NET project
        run: dotnet build --configuration Release

      - name: Create build directory
        run: mkdir -p build

      - name: Export Game
        run: godot --headless --path src/InertiCorp.Game --export-release "Linux" "../../build/InertiCorp.x86_64"

      - name: Copy .NET assemblies to build
        run: |
          DATA_DIR="build/data_InertiCorp.Game_linuxbsd_x86_64"
          mkdir -p "$DATA_DIR"

          # The ExportRelease directory contains linux-x64 subdirectory with all assemblies
          # Copy the contents directly (not the linux-x64 folder itself)
          EXPORT_DIR="src/InertiCorp.Game/.godot/mono/temp/bin/ExportRelease/linux-x64"
          if [ -d "$EXPORT_DIR" ]; then
            cp -r "$EXPORT_DIR"/* "$DATA_DIR/"
            echo "Copied assemblies from: $EXPORT_DIR"
          else
            echo "ExportRelease/linux-x64 not found, checking other locations..."

            # Fallback to Release directory
            MONO_DIR="src/InertiCorp.Game/.godot/mono/temp/bin/Release"
            if [ -d "$MONO_DIR" ]; then
              cp -r "$MONO_DIR"/* "$DATA_DIR/" || true
              echo "Copied assemblies from: $MONO_DIR"
            fi
          fi

          echo "Data directory file count:"
          ls -1 "$DATA_DIR" | wc -l
          echo "Sample files:"
          ls "$DATA_DIR" | head -10

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create tarball
        run: |
          cd build
          tar -czvf "../InertiCorp-${{ steps.version.outputs.VERSION }}-Linux.tar.gz" *

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          files: InertiCorp-${{ steps.version.outputs.VERSION }}-Linux.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
